{
  "rules": {
    "payment_pending": {
      "$orderCode": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['uid', 'plan', 'amount', 'createdAt']) && newData.child('uid').isString() && newData.child('uid').val().length > 0 && newData.child('uid').val().length < 120 && newData.child('plan').isString() && newData.child('amount').isNumber() && newData.child('amount').val() > 0 && newData.child('amount').val() < 100000001"
      }
    },
    "payment_logs": {
      "$orderCode": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['uid', 'plan', 'amount', 'at'])"
      }
    }
  }
}
