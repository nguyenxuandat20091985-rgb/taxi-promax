{
  "name": "taxi-promax",
  "version": "1.0.0",
  "description": "Hệ thống Taxi ProMax",
  "main": "api/create-payment.js",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "dev": "vercel dev",
    "test": "node scripts/flow-contract-test.mjs && node scripts/security-smoke-test.mjs && node scripts/diagnostic-contract-test.mjs",
    "check": "for file in api/*.js lib/*.js js/**/*.js; do node --check \"$file\" || exit 1; done",
    "build": "npm run check && rm -rf public && mkdir -p public && cp index.html khachhang.html xeghep.html admin.html manifest.json sw.js config.xml autonomous-client.js public/ && cp -R css js public/"
  },
  "dependencies": {
    "@payos/node": "^1.0.10",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
